# v2.7.1 发布前审查

日期：2026-09-17。状态：本地正式签名候选包已生成，尚未公开发布；真机验收待完成。

## 产品评价

快捷入口轻量、离线且用途明确，适合频繁在多个平台取件的用户。它负责跳转，不能汇总平台账号里的包裹数据；第三方内部链接变化后，可能只能打开首页。

OCR 的价值取决于能否减少用户操作。当前需要主动选图，且只能识别连续数字，适合作为辅助实验功能。字母、分段码、多条通知和复杂整屏截图尚未充分支持，不宜宣传为通用取件码识别器。合成数据上的高分不能证明实拍或真实通知截图的可靠性。

## 本次修正

| 问题 | 影响与修改 |
| --- | --- |
| 候选评分调用 `to28` 时宽度为 0 | 读错像素；改为传入真实图像宽高，新增带偏移数字的全流程测试 |
| 二值化阈值使用严格小于 | 纯黑白图片可能丢失前景；修正阈值边界并覆盖深色与透明背景 |
| 相邻字符过度合并、枚举短子串 | 可能合并数字或截短完整号码；改为保留完整字符组，拒绝超过 8 个字符的组 |
| Android 6.0 调用较新排序 API | OCR 可能运行失败；改为兼容 API 23 的排序方法 |
| 网页兜底传入空 destination 后解引用 | 存在崩溃路径；补上空值分支，并以实际启动结果判断能否打开 |
| OCR 无任务和页面生命周期保护 | 重复识别、大图、退出后弹窗存在问题；增加忙碌状态、尺寸限制、取消与页面状态检查 |
| 结果难以核对修改 | 改为可编辑候选码，检查 4–8 位数字后复制；移除容易被误解为准确率的置信度展示 |
| 快捷入口和设置刷新缺口 | 补充 `onNewIntent`、动态快捷方式 action、系统数量上限及首页排序刷新 |
| 桌面按钮过密 | 改为每行最多三个按钮 |
| 发布流程缺少测试且可退回调试签名 | 增加测试、Lint、版本标签检查，缺少正式签名时停止发布；新增 PR/main 验证流程 |

## 验证证据

- `testDebugUnitTest lintRelease assembleRelease`：成功。
- 11 项 JUnit 测试全部通过：10 项 Java 图片流程测试，1 项 Java/PyTorch 模型输出对照测试。
- 图片流程覆盖 4–8 位完整码（含前导零）、非零坐标、纯黑白、深色背景、紧邻数字、透明背景、长号码拒绝、多行长号码与候选码、空图、过多组件和取消。
- Lint：0 errors、9 warnings（现有未使用字符串资源）；另有快捷方式使用统计建议。
- Release 工作流的两个 shell 段语法检查通过；已在临时目录实际生成 v2.7.1 发布说明和 SHA-256。
- APK：包名 `cn.pickup.launcher`，版本 `2.7.1`，versionCode `32`，minSdk `23`。
- APK 签名验证通过；证书 SHA-256 与目录中的 v2.7.0 相同：`25da45ad85bdfc6d27bb2823f49ba44578e05ec6ab6fac6218e558a1e54ccf1c`。
- 候选 APK SHA-256：`cc3e99010687bad5c77ca595e706555bf4419151fe846502df07effa077c2e3d`。

本机 JDK 17 的 Windows Unix-domain socket 连接失败。此次仅为构建进程设置 `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=NUL`，使其回退到 TCP；没有改系统网络配置。该 Windows 临时解决方式不应放入 Linux CI。

## 真机验收待办

`adb devices` 当前没有设备。以下行为未做手机端实测，不将代码审查或 JVM 测试算作真机通过：

- [ ] 保留旧版数据，使用候选 APK 覆盖安装 v2.7.0。
- [ ] 从系统选图器选择裁剪的 4/6/8 位数字截图，核对完整结果和前导零。
- [ ] 修改候选码，点击复制及长按复制，确认剪贴板为修改后的值。
- [ ] 识别中返回、切后台、旋转屏幕、重新进入；确认没有崩溃或旧页面弹窗。
- [ ] 检查小屏、大字体及 Android 15 下按钮、结果框与系统栏显示。
- [ ] 逐一测试已安装的平台 App；也测试未安装平台、存在浏览器及无可用处理程序的情况。
- [ ] 首页已打开时触发桌面快捷入口；改变排序后返回首页，确认立即刷新。

建议真机验收后再公开发布，OCR 保留“实验功能”说明。后续优先收集用户授权的脱敏截图，建立包含文字、分段码与多条通知的评测集，再决定是否更换 OCR 方案或增加区域裁剪。

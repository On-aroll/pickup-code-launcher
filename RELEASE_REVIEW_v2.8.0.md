# v2.8.0 发布候选验收

验证日期：2026-09-17；2026-09-18 用户授权公开发布。本文记录本地签名候选包与自动化验证基线；实际发布产物及其校验值以 GitHub Release 为准，真机验收仍未完成。旧版 v2.7.1 的审查仅为历史记录，当前功能以本文和 README 为准。

## 产品修正

旧版只识别连续数字，再让用户复制，不能解决多件、多个地点的实际取件问题。本版将核心改为「收集通知 → 提取多件 → 核对对应关系 → 按地点归纳 → 取完标记」。

- 截图改用随包内置的中文 OCR，读取字母、数字、横杠和中文地点；批量最多 30 张，也支持粘贴多条文字、手动补录。
- 取件码不再限制为 4–8 位数字，保留 `2-103-4567`、`A-02-0038`、前导零；手机号、长运单号及日期不会默认当成码。
- 识别取件点、地址、快递公司；多个码拆为多条，清晰交替的卡片字段依序匹配，无法确认的关联留空并提示，不编造地点。
- 首页面向待取清单：总件数/地点数、本站所有码、搜索、分组复制、总表复制、统一地点、已取与恢复。原平台入口保留。
- 所有导入先待核对；完整无冲突项可预览后批量确认，其余逐件修改。不同分店不会仅凭连锁名合并，叫法不一致时可统一名称和地址。
- 完全重复的活动通知跳过；不同通知中相同地点相同码仅提示重复，保留共用码的可能；手工修改不会被同一通知再次导入覆盖。
- SQLite 持久化；组内修改使用事务。正在识别时旋转页面不取消后台任务，已保存的草稿在进程退出后仍存在。

## 验证结果

- `testDebugUnitTest lintRelease assembleRelease`：BUILD SUCCESSFUL。
- 37 项测试通过：18 项文本提取/归纳/去重规则；8 项 Robolectric 数据库与页面流程；10 项旧数字处理回归；1 项旧模型对照。旧数字测试不代表新中文 OCR 像素识别已实测。
- 页面流程覆盖：核对并修改码、加入待取、已取/恢复、多选图 Intent、启动首页路由、旋转后恢复未提交编辑、整组地点修改。
- 数据库覆盖：关闭重开保留字段与状态、重复导入不覆盖修改、完成后重用码、同码两条通知独立保存。
- Lint：0 errors、14 warnings（9 个历史未用字符串、3 个平台 EXIF API 建议、2 个文字资源国际化建议），另有 1 个快捷方式统计建议。未将警告隐藏或当作零警告。
- APK 包名 `cn.pickup.launcher`；版本 `2.8.0` / versionCode `33`；minSdk `23`，targetSdk `35`。
- APK 签名 v1/v2 校验通过，签名证书与 v2.7.0 相同：`25da45ad85bdfc6d27bb2823f49ba44578e05ec6ab6fac6218e558a1e54ccf1c`。
- APK 合并权限检查：无 INTERNET、ACCESS_NETWORK_STATE、READ_SMS、通知监听或全相册读取权限；只有 AndroidX 生成的应用内部 signature 权限。
- APK 内已核实包含中文/拉丁文字模型和 arm64-v8a、armeabi-v7a、x86、x86_64 OCR 库。
- 候选文件 `../pickup-code-launcher-v2.8.0.apk`；46,688,208 字节（约 44.5 MiB）。
- APK SHA-256：`68000b864bc08c07d76dc3fb536a402d58d2429881a3e993ab1f23e73e99c18d`。

## 未完成的真机验收

当前 `adb devices` 无设备。以下项目不能用 JVM 测试或源码审查代替，发布前必须补做：

- [ ] 使用原签名覆盖安装 v2.7.0，确认原快捷入口设置保留。
- [ ] 飞行模式首次启动，导入真实清晰截图，确认中文模型无需下载、无 Google Play 服务设备是否正常。
- [ ] 分别测试柜码、字母货架码、分段货架码、多个地点整屏截图；逐件比对原图，统计漏识别/错误关联，不只看有没有输出。
- [ ] 一次选择多张及长截图，包含重复截图与同码不同地点，确认件数和分组符合原通知。
- [ ] 识别中返回、旋转、切后台及主动停止；进程被杀后重新导入，确认已完成草稿保留。
- [ ] 在小屏、大字体、Android 15 下检查按钮、弹窗、系统栏；测试已取/恢复、复制码、本站清单、总表。
- [ ] 将 `examples/取件通知示例.txt` 全部粘贴，核对后应得到 6 件、3 个取件点（3 / 2 / 1 件）。
- [ ] 检查已安装/未安装的第三方平台入口、桌面长按菜单与小组件。

## 仍有的局限

归纳依赖明确标签、文字结构与保守规则，不是任意通知的语义理解或账号同步。复杂多列截图、缺失地点、稀疏字段、模糊字符、极长截图缩小等仍可能识别或关联错误，导入后一律待核对。没有真实截图集准确率，不引用旧数字模型的合成指标宣传新功能。

名称和地址必须规范一致才会自动合组；不会根据相似名字猜测是否同一家。没有地图路线规划、自动读取短信、平台订单同步、扫码开柜或自动提交码。应用被系统杀死后，未完成导入不会自动续传；本机清单卸载后丢失，当前只有文字清单复制，不提供可恢复的文件备份。

本机 Windows/JDK 构建沿用一次性 `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=NUL`，处理该环境的 Unix-domain socket 失败；未写入跨平台 CI。
